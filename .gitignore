__pycache__/
*.pyc
*.tmp

# Сборки доски. До 09.09 их тут не было, потому что доска собиралась только в
# CI и попадала в docs/. Теперь каталог — рабочая копия: board.html и hub.html
# появляются при каждом локальном прогоне, и коммитить их значит хранить два
# ответа на один вопрос. Отдаётся docs/, собирается — здесь.
dashboard/board.html
dashboard/hub.html
dashboard/board.html.prev
**/__pycache__/
